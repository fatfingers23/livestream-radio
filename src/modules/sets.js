const defaultState = {
    currentSet: [],
  };
  
  const actions = {

  };
  
  const mutations = {

  };
  
  const getters = {
      currentSet: state => state.currentSet,
  };
  
  export default {
    state: defaultState,
    getters,
    actions,
    mutations,
  };