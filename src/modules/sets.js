const defaultState = {
    currentSet: [],
    currentStation: []
  };
  
  const actions = {

  };
  
  const mutations = {

  };
  
  const getters = {
      currentSet: state => state.currentSet,
      currentStation: state => state.currentStation
  };
  
  export default {
    state: defaultState,
    getters,
    actions,
    mutations,
  };